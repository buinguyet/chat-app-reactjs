import React from "react";
import TypeMessageComponent from "../../components/TypeMessage/typeMessage";

const TypeMessageContainer = (props) => {
  return <TypeMessageComponent />;
};

export default TypeMessageContainer;
